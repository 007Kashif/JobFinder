import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

import MainNavigator from './src/boot/MainNavigator'

const App = () => {
  return (
   <MainNavigator />
  )
}

export default App

const styles = StyleSheet.create({})